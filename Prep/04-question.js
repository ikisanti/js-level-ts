//! What are js primitives
//? These are not an object and are immutable
//?immutable refers to data that cannot be changed once created

// string

// number

// boolean

// undefined

// null

// bigint

// symbol

//! Primitives cant be modify internally
let name = 'Peter Parker'
console.log('name:', name[0]);

name[0] = 'L'
console.log('name:', name[0]);